package main

import "fmt"

func main() {
//  map mp

mp := map[uint]uint{}
n :=uint(100)

for number := 1; number <= n; number++ {
	for d := unit(1); d <= 5; s++ {
		if number % d == 0 {
			mp[d]++
		}
	}
}
}
